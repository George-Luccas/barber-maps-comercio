import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.barbermaps.app',
  appName: 'barber-maps-comercio',
  webDir: 'out', // <--- TEM QUE SER 'out
};

export default config;
